using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MessageBoard.Models
{
    static class MessageRepository
    {
        static MessageRepository()
        {
            
        }

        //public static List<Message> Messages { get; set; }

        //public static void NewMessage(Message message)
        //{
        //    message.Id = ();
        //    message.PostDate = DateTime.Now;
        //    Messages.Add(message);
        //}

        //public static void UpdateMessage(Message message)
        //{
        //    var oldMessage = Messages.Where(m => m.Id == message.Id).FirstOrDefault();
        //    oldMessage.MessageTitle = message.MessageTitle;
        //    oldMessage.Description = message.MessageTitle;
        //}
    }
}
